# zRAM Compression Algorithm
#COMP_ALGO="lzo"

# Number of zRAM Devices
#DEV_NUM="$(grep -c '^processor' /proc/cpuinfo)"

# (origin) Log directory to be RAM Log
#LOG_DIR="/var/log"

# Size of RAM Log (in kB)
#LOG_SIZE_KB=$((128*1024))

# Log Compression Algorithm
#LOG_COMP_ALGO="lz4"
